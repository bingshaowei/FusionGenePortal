# Fusion Gene Portal - Frontend

> **Note:** The frontend source code (`src/` directory) is not included in this repository.
> Only the production build (`build/`) is committed.
>
> To rebuild the frontend, restore the React source code to a `src/` directory
> and run `npm run build`.
